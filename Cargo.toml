[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.15.0"
serde = { version = "1.0.229", features = ["derive"], optional = true }

[dev-dependencies]
sha = "1.0.3"

[features]
serde = ["dep:serde"]
